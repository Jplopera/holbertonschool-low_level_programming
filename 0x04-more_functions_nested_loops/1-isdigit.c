#include "main.h"
/**
 * _isdigit - search numbers
 * @c: int variable
 * Return: 1 if number 0 if not
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return; (0);
	}
}
